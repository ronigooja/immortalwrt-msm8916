#!/bin/sh

changed=0

for section in $(uci -q show wireless | sed -n "s/^wireless\.\([^=]*\)\.mode='ap'$/\1/p"); do
	uci set wireless."$section".disabled='1'
	changed=1
done

[ "$changed" = "1" ] || exit 0

uci commit wireless
wifi reload 2>/dev/null || wifi down 2>/dev/null || true

exit 0
